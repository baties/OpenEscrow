{
  "name": "@open-escrow/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "OpenEscrow Solidity smart contracts — Hardhat + OpenZeppelin",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "test:contracts": "hardhat test",
    "lint": "solhint 'src/**/*.sol'",
    "export-abi": "hardhat run scripts/export-abi.ts",
    "clean": "hardhat clean",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network mainnet",
    "deploy:bsc": "hardhat run scripts/deploy.ts --network bsc",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon",
    "upgrade:sepolia": "hardhat run scripts/upgrade.ts --network sepolia",
    "upgrade:mainnet": "hardhat run scripts/upgrade.ts --network mainnet",
    "upgrade:bsc": "hardhat run scripts/upgrade.ts --network bsc",
    "upgrade:polygon": "hardhat run scripts/upgrade.ts --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "@openzeppelin/contracts-upgradeable": "^5.6.1",
    "@openzeppelin/hardhat-upgrades": "^3.9.1",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0",
    "solhint": "^6.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
